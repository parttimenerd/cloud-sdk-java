name: "cache-maven-dependencies (disabled in demo fork)"
# Disabled in demo fork — requires SAP-internal secrets/infra.
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in demo fork."
