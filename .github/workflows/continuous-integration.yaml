name: "Continuous Integration (disabled in demo fork)"
# This workflow is from the upstream SAP/cloud-sdk-java repo and requires
# SAP-internal secrets and infrastructure. Disabled in this demo fork.
on:
  workflow_dispatch:
    inputs:
      _note:
        description: "This workflow is disabled in the demo fork."
        required: false
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the demo fork. See test-order-ci.yml instead."
